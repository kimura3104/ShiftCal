<p id="notice"><%= notice %></p>

<h1>従業員一覧</h1>


<div class="main-page">
  <div class="main-content">

<table>
  <thead>
    <tr>
      <th>従業員名</th>
      <th>時給</th>
      <th colspan="2">操作</th>
    </tr>
  </thead>

  <tbody>
    <% @employees.each do |employee| %>
      <tr>
        <td><%= employee.name %></td>
        <td><%= employee.wage %>円</td>
        <td colspan="2"><%= link_to '編集', edit_employee_path(employee) %> <%= link_to '削除', employee, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '新規登録', new_employee_path %>

  </div>
  </p>
  <%= link_to 'Back', admin_path %>
</div>
